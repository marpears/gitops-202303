---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
- aws-secret-manager-test-app 
patches:
- target:
    kind: Application
    name: cert-manager-operator
  patch: |-
    - op: replace
      path: /spec/source/path
      value: './manifests/cert-manager/overlays/dev'
- target:
    kind: Application
    name: openshift-logging
  patch: |-
    - op: replace
      path: /spec/source/path
      value: './manifests/openshift-logging/overlays/dev'
